{
  "name": "@rimtrans/io",
  "description": "The io library for RimTrans.",
  "version": "0.3.0",
  "private": true,
  "main": "lib/io.js",
  "types": "types/io.d.ts",
  "scripts": {
    "clean": "rimraf ./types && rimraf ./lib",
    "build:types": "tsc",
    "build:babel": "babel ./src -x .ts -d ./lib",
    "build": "yarn build:types && yarn build:babel",
    "test": "jest"
  },
  "dependencies": {
    "@types/globby": "^9.1.0",
    "@types/ncp": "^2.0.1",
    "@types/rimraf": "^2.0.2",
    "globby": "^9.2.0",
    "ncp": "^2.0.0",
    "rimraf": "^2.6.3"
  }
}
