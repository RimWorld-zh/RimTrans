{
  "name": "@rimtrans/io",
  "description": "The io distrary for RimTrans.",
  "version": "0.4.0",
  "private": true,
  "main": "dist/cjs/io.js",
  "module": "dist/esm/io.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build:cjs": "babel src -x .ts -d dist/cjs -s",
    "build:esm": "babel src -x .ts -d dist/esm -s --esm",
    "build:types": "tsc && ncp dist/types dist/cjs && ncp dist/types dist/esm && rimraf dist/types",
    "build": "yarn build:cjs && yarn build:esm && yarn build:types",
    "test": "jest --maxWorkers=1 --setupTestFrameworkScriptFile=../jest.setup.js"
  },
  "dependencies": {
    "@types/globby": "^9.1.0",
    "@types/ncp": "^2.0.1",
    "@types/rimraf": "^2.0.2",
    "globby": "^9.2.0",
    "ncp": "^2.0.0",
    "rimraf": "^2.6.3"
  },
  "devDependencies": {
  }
}
